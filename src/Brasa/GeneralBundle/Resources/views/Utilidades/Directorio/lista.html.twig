{% extends '::base.html.twig' %}
{% block title %}Gestor de archivos lista - Soga App{% endblock %}
{% block content %}
    <div class="container">
    <div class="page-header">    
        <h3>Listado directorio gestor de archivos</h3>
    </div>
    {{ form_start(form, { 'attr': {'class': 'form-horizontal'} }) }}
    {{ form_errors(form) }} 
    <table class="table table-striped table-bordered table-condensed table-hover">
        <tr>
            <th>CÓDIGO</th>
            <th>RUTA</th>
            <th>{{ knp_pagination_sortable(arDirectorios, 'NOMBRE', 'codigo') }}</th> 
            <th></th>
            <th></th>
            <th></th>
        </tr>
        
            {%for arDirectorios in arDirectorios %}
               <tr>
                <td>{{arDirectorios.codigoDirectorioPk}}</td>
                <td>{{arDirectorios.ruta}}</td>
                <td>{{arDirectorios.nombre}}</td>
                <td><a href="{{ path('brs_gen_directorio_nuevo',{'codigoDirectorio': arDirectorios.codigoDirectorioPk})}}"><img src="{{ asset('imagenes/bootstrap/glyphicons_151_edit.png') }}" width=20 height=20 title="Editar el registro"></a></td>
                <td style="text-align: center"><a href="{{ path('brs_gen_directorio_archivos', { 'codigoDirectorio': arDirectorios.codigoDirectorioPk }) }}"><img src="{{ asset('imagenes/bootstrap/glyphicons_144_folder_open.png') }}" width=22 height=22 title="Detalle del registro"></a></td>                
                <td><input type="checkbox" name="ChkSeleccionar[]" value="{{ arDirectorios.codigoDirectorioPk }}" /></td>
            {%endfor%}
            </tr>            
    </table>
    {{ knp_pagination_render(arDirectorios) }}
    <div class="btn-toolbar" style="float: right">              
        <div class="btn-group">                
            {{ form_widget(form.BtnExcel, { 'attr': {'class': 'btn btn-default btn-sm'} }) }}                
        </div>
        <div class="btn-group">
            <a class="btn btn-default btn-sm" href="{{ path('brs_gen_directorio_nuevo', {'codigoDirectorio': 0}) }}">Nuevo</a>
        </div>
        <div class="btn-group">
            {{ form_widget(form.BtnEliminar, { 'attr': {'class': 'btn btn-danger btn-sm'} }) }}</td>                   
        </div>
    </div>     
    {{ form_rest(form) }}
    {{ form_end(form) }}
    </div>
{% endblock %}