{% extends '::base_sin_menu.html.twig' %}
{% block title %}Detalle liquidacion nuevo - Soga App{% endblock %}
{% block content %}
<div class="container">
    {{ form_start(form, { 'attr': {'class': 'form-horizontal'} }) }}
    <h3>Liquidaciones</h3>
        <table class="table table-striped table-bordered table-condensed table-hover">
        <tr>            
            <th>CÓDIGO</th>
            <th>FECHA</th>
            <th>EMPLEADO</th>    
            <th>BANCO</th>
            <th>CUENTA</th>
            <th style="text-align: right">NETO</th>
            <th><input type="checkbox" name="ChkSeleccionarTodos" OnClick="ChequearTodos(this);"/></th>
        </tr>        
            {%for arLiquidaciones in arLiquidaciones %}
               <tr>
                <td>{{arLiquidaciones.codigoLiquidacionPk }}</td>
                <td>{{ arLiquidaciones.fecha |date("Y/m/d")  }}</td>
                <td>{{arLiquidaciones.empleadoRel.nombreCorto }}</td>
                <td>{{arLiquidaciones.empleadoRel.bancoRel.nombre |default('') }}</td>
                <td>{{arLiquidaciones.empleadoRel.cuenta |default('') }}</td>                
                <td style="text-align: right">{{ arLiquidaciones.vrTotal | number_format(0, '.', ',') }}</td>
                <td><input type="checkbox" name="ChkSeleccionar[]" value="{{ arLiquidaciones.codigoLiquidacionPk }}" /></td>
            {%endfor%}
            </tr>            
        </table> 
     
    
    <div class="btn-toolbar" style="float: right">
        <div class="btn-group">                                
            {{ form_widget(form.BtnGuardar, { 'attr': {'class': 'btn btn-primary btn-sm'} }) }}
        </div>  
    </div>   
    {{ form_end(form) }}
{% endblock %}