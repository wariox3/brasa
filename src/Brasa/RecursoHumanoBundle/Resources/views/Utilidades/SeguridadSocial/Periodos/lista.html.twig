{% extends '::base.html.twig' %}
{% block content %}
<div class="container">
    <div class="page-header">
        <h3>Lista selecciones</h3>
    </div>
    {{ form_start(form, { 'attr': {'class': 'form-horizontal', 'novalidate': 'novalidate'} }) }}
        {{ form_errors(form) }}                        
        <table  class="table table-striped table-bordered table-condensed table-hover">            
            <tr>
                <th>COD</th>
                <th>AÑO</th>
                <th>MES</th>
                <th>GENERADO</th>
                <th></th>
            </tr>
            {% for arSeguridadSocialPeriodos in arSeguridadSocialPeriodos %}
            <tr>
                <td><input type="hidden" name="LblCodigo[]" value="{{ arSeguridadSocialPeriodos.codigoSeguridadSocialPeriodoPk }}">{{ arSeguridadSocialPeriodos.codigoSeguridadSocialPeriodoPk }} </td>
                <td>{{ arSeguridadSocialPeriodos.anio  }}</td>
                <td>{{ arSeguridadSocialPeriodos.mes  }}</td>
                <td {% if arSeguridadSocialPeriodos.estadoGenerado == 1 %}class="success"{% endif %}>{% if arSeguridadSocialPeriodos.estadoGenerado == 1 %} SI {% else %} NO {% endif %}</td>                                
                <td><input type="checkbox" name="ChkSeleccionar[]" value="{{ arSeguridadSocialPeriodos.codigoSeguridadSocialPeriodoPk }}" /></td>
            </tr>
            {% endfor %}                        
        </table>                       
        {{ knp_pagination_render(arSeguridadSocialPeriodos) }}    


        <!-- Botones Menu Items -->
        <div class="btn-toolbar" style="float: right">                 
            <div class="btn-group">
                {{ form_widget(form.BtnGenerar, { 'attr': {'class': 'btn btn-default btn-sm'} }) }}              
            </div>
        </div>          
        <!-- Fin Botones Menu Items -->
        {{ form_rest(form) }}
    {{ form_end(form) }}   
    
</div>
{% endblock %}
