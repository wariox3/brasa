{% extends '::base_sin_menu.html.twig' %}
{% block content %}
    <div class="left_articles">                
        <p class="description"><h2>Seleccione el movimiento</h2></p>          
        <table class="table table-striped table-bordered table-condensed">

            <tr>
                <th>ID</th>
                <th>Número</th>
                <th>Fecha</th>
                <th>Cliente</th>
                <th>Documento</th>
                <th>Total</th>
                <th></th>
            </tr>
                {% for movimiento in arMovimientos %}
            <tr>
                <td>{{ movimiento.codigoMovimientoPk }}</td>
                <td>{{ movimiento.numeroMovimiento | number_format(0)}}</td>
                <td>{{ movimiento.fecha |date("m/d/Y")  }}</td>
                <td>{{ movimiento.nombreCortoTercero }}</td>  
                <td>{{ movimiento.nombre }}</td>  
                <td style="text-align: right">{{ movimiento.total | number_format(2, '.', ',')}}</td>
                <td style="text-align: center"><a href="{{ path('inventario_movimientos_agregar_item_documento_detalle', { 'codigoMovimiento': movimiento.codigoMovimientoPk,  'codigoMovimientoOrigen': intCodigoMovimientoOrigen} ) }}"><IMG SRC="{{ asset('bundles/zikmont/imagenes/tema1/glyphicons_152_new_window.png') }}" WIDTH=18 HEIGHT=18></a></td>
            </tr>
                {% endfor %}            
        </table>         
    </div>
{% endblock %}